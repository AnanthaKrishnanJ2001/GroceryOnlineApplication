# GroceryOnlineApplication
A Spring Boot RESTful application implementing an online grocery system with CRUD operations for products and customers, order processing using DTOs, request validation, and clean separation of concerns across controller and service layers.
